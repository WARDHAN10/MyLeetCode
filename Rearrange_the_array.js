function rearrangeNaive(arr) {
  for (let i = 0; i < arr.length; i++) {
      if(arr[i] > 0 ){
          let j = i  
          while(arr[j] > 0 ){
            
          }
      }
  }
}

console.log(rearrangeNaive([1, 3, 0, 2]));
